race_node:
  ros__parameters:
    laps: 7
    max_retries: 20

    # use_start_for_first_segment=False : le planner prend la pose actuelle du robot via TF sinon il relie les points.
    use_start_for_first_segment: false

    tracks_file: "/home/voiture/bolide_voiture/config_nav2/tracks.yaml"
    active_track: "map_etage_2_cleaned"
